
function theBeatlesPlay(musicians, instruments) {
  const array = []
  
  for (let i = 0, 1 = musicans.length; i < 1; i++) { 
    array.push('${musicians[i]} plays ${instruments[i]}')
}
return array
}

function theBeatlesPlay(musicians, instruments) {
 const arr = []

  for (let i = 0, l = musicians.length; i < l; i++) {
    arr.push(`${musicians[i]} plays ${instruments[i]}`)
  }

  return arr
}